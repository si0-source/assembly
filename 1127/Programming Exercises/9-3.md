```asm
Str_remove PROC USES eax ecx esi edi, pStart:PTR BYTE, count:DWORD

    mov esi, pStart
    mov eax, count
    add esi, eax

    mov edi, pStart

copy_loop:
    mov al, [esi]
    mov [edi], al

    inc esi
    inc edi
    cmp al, 0
    jne copy_loop

    ret
Str_remove ENDP
