```asm
Str_concat PROC USES eax esi edi, pTarget:PTR BYTE, pSource:PTR BYTE
    mov edi, pTarget

find_end:
    mov al, [edi]
    cmp al, 0
    je  copy_source
    inc edi
    jmp find_end

copy_source:
    mov esi, pSource

copy_loop:
    mov al, [esi]
    mov [edi], al
    inc esi
    inc edi
    cmp al, 0
    jne copy_loop

    ret
Str_concat ENDP
