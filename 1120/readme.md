스택이라는 메모리 영역을 확보하면서 시작  
프로시저가 확보되면 자동으로 스택 프레임이 생성된다  
C언어는 메모리 영역을 확보해두고 실행해야된다 대이터 힙 스택에 프레임  
메인 스택 프레임 다른걸 실핼하면 위에 계속 스택이 쌓인다  
메소드를 만들때 괄호안에 들어가는 거 파라미터 호출할때 아규먼트  
인수 인자 인수를 더 많이 쓴다  
리턴이 자동으로 스택에 저장  
레지스터 값을 프로시저를 호출하기 전에 푸시하거나 프로시저안에서 푸시하고 팝하면서 대피시킨다  
BP 베이스 레지스터 SP 스택 포인트  
스택의 기준 점이 스택 새그먼트 CS BP SP 스택관련된 3개
가급적이면 푸시팝하면서 번거롭게 레지스터를 쓰지말자
call by 레퍼런스는 주소가 들어간다
passing array는 하이레벨과 비슷하다
푸시했으면 꼭 팝도 하기  
ret은 리턴 어드레스
int a;같은 변수안에 사용하는 걸 로컬 베리어블이라 부른다   
배열이나 클래스로 만든 건 힙영역에 만들어지는 객체  
변수는 전부 스택에 들어간다  
enter는 자동으로 만들어준다
enter쓰면 leave를 써야한다
local은 lavel : type으로 빠르게 가능
스택 그린다고 생각하면 BP 파라미터 로컬 베리어블을 생각할 것  
스택은 EBP 파라미터는 + 로컬 베리어블은 - 또 스택을 만들면 SP가 가리키는 쪽  
ADDR은 주소구하는 거 모조건 invoke다음에 온다
ret은 자동으로 리턴시킨다
invoke 는 자동으로 스택을 정리하고 
실행파일을 만드는 건 링커  
파라미터가 여럿이면 @뒤에 숫자 붙이기
외부에서 들고올때extern 허락할때 public
