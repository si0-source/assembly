variable = expression  
    assignment  
input p o  
memory register memory  
변수와 함수의 묶음이 클래스  
control statemaent  
  if, switch  
  for,while, do~while  
  break, continue  
  return  
  goto(특이하다)  
expression 구성하는 4가지  
constant, literal  
variable  
operator  
function, method  
function은 무조건 return으로 가져오고 리턴 뒤에는 항상 수식이 있다  
파이썬에서는 모듈  
Structures 구조체 어셈블리에서 나옴  
서로 연관성있는 변수들의구분  
union  
coord 스트럭쳐 서로 다른 변수로 공유 그것이 유니온  
ALIGN 찻기위해  
union 합치기  
c언어와 어셈블리어의 매크로 차이점과 그 특징  
c언어 #define이라는 태그를 앞에 붙여서 만드는 간단한 매크로 길게 쓰기 보다는 간단한 코드 한 줄을 여러 군데에 쓸 수 있게 등록하는 느낌  
어셈블리어
